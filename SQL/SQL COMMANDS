CREATE DATABASE <Имя базы>; - Cоздание базы данных

DROP DATABASE <Имя базы>; - удаление базы данных
DROP TABLE <Имя таблицы>; - удаление таблицы

CREATE TABLE <Имя таблицы> (<Название столбца> <Тип данных>, ..) =>
Создание таблицы и указание типа столбцов в ней

SHOW COLUMNS FROM <Имя таблицы>; - Посмотреть содержимое таблицы
SHOW TABLES; - Посмотреть в существующие таблицы внутри базы

INSERT INTO <Название таблицы> (<Название столбца>) VALUES (<значение>); =>
добавление в столбец таблицы новое значение
UPDATE <Название таблицы> SET <Название столбца> = <Значение>; =>
Обновить значение столбца в таблицы (Поменять уже сущ. значение на другое)
DELETE FROM

SELECT <столбец, который нужно извлечь> FROM <Название таблицы>; =>
Выбрать значение из столбца выбранной таблицы
SELECT DISTINCT .. - Выбрать все уникальные значения из столбцов таблицы

WHERE {Условие}; - Условие для конкретики
LIKE {Шаблон}; - поиск значения в столбцах таблицы по шаблону
ORDER  BY <Столбец> - отсортировать значения указанного столбца
ORDER BY .. DESC - отсортировать по убыванию
ORDER BY .. ASC - отсортировать по возрастанию
<Столбец по которому ищем> IN(<1 вариант>, <2 вариант>) =>
указывает на совпадение в столбце, возвращая booleang

LIMIT <max> - указываем, сколько всего взять столбцов в выборку
GROUP BY <Имя поля> - группировка по конкретному критерию
(INNER) JOIN <Таблица> ON <Условие связывания> - Связывание таблиц
LEFT JOIN .. - связывание, но с условием если элементу соответствует null, то поля все равно указываются
UNION - объединяет данные из неск. таблиц, оставляя только уникальные значения

Функции в sql
count() - подсчет числа записей
count() as count - так программа будет считать кол-во по заданной переменной
max() - нахождение максимального значения для указанного поля
avr() - вычисление среднего арифметического указанного поля
min() - нахождение минимального значения для указанного поля






